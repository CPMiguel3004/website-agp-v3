---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class={`double-divider ${className}`} role="separator" aria-hidden="true">
  <span></span>
  <span></span>
</div>
